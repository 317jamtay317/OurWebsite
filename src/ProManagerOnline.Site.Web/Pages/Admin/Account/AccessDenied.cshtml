@page
@{
    ViewData["Title"] = "Access denied";
}

<div class="container-site flex justify-center py-12 sm:py-16">
    <div class="card w-full max-w-md text-center">
        <h1 class="text-xl font-bold text-slate-900">Access denied</h1>
        <p class="mt-2 text-sm text-slate-600">You don't have permission to view that page.</p>
        <a class="btn-secondary mt-6 inline-flex" asp-page="/Admin/Index">Go to dashboard</a>
    </div>
</div>

{
  "name": "promanageronline-site-web",
  "version": "1.0.0",
  "private": true,
  "description": "Front-end build (Tailwind CSS) for the ProManager Online Razor site.",
  "scripts": {
    "build:css": "tailwindcss -i ./Styles/app.css -o ./wwwroot/css/app.css --minify",
    "watch:css": "tailwindcss -i ./Styles/app.css -o ./wwwroot/css/app.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}
